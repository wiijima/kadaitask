<h1>
    id : <%= @task.id %>のTask編集ページ
</h1>

<%= form_for(@task) do |f| %>
    <%= f.label :content, 'Task' %>
    <%= f.text_field :content %>
    <%= f.submit '投稿' %>
<% end %>

<%= link_to 'Task一覧に戻る', tasks_path %>

<%= link_to 'Taskの詳細ページに戻る', task_path(@task) %>
